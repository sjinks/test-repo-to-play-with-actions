name: Debug

on:
  push:
    tags:
      - '*'

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"

  more_debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git fetch --depth=1 origin +refs/tags/*:refs/tags/*
      - run: git tag -l --points-at ${{ github.sha }}
      - run: git tag -l --points-at ${{ github.event.after }}
      - run: git for-each-ref "${{ github.ref }}" --format="%(subject)"
      - run: git for-each-ref "${{ github.ref }}" --format="%(*subject)"
      - run: git for-each-ref "${{ github.ref }}" --format="%(body)"
      - run: git for-each-ref "${{ github.ref }}" --format="%(*body)"
      - run: git rev-list --objects -g --no-walk --all
      - run: git tag -l --points-at ${{ github.sha }} --format="%(subject)"
      - run: git tag -l --points-at ${{ github.sha }} --format="%(body)"
      - run: git tag -l --points-at ${{ github.sha }} --format="%(*subject)"
      - run: git tag -l --points-at ${{ github.sha }} --format="%(*body)"

# github.event.after: refers to the tag itself
# github.event.forced
# github.event.created === true
